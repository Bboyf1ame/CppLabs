#include <iostream> 
#include <math.h>
#include <conio.h>
using namespace std;
int main()
{
	double x, y, z, a, b;
	cout << x;
	cout << y;
	cout << z;
	a = log(fabs(y - (sqrt(x))(x - y / ((z + pow(x, 2)) / 4))));
		b = x - (pow(x, 2) / 6) + ((pow(x, 5)) / 120);
	system("pause");
	return 0;
}